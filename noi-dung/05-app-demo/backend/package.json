{
  "name": "booking-management-backend",
  "version": "1.0.0",
  "description": "Hệ thống quản lý đặt phòng - Backend API Server",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Backend does not need build step'",
    "seed": "node scripts/seed.js",
    "db:create": "sqlcmd -S localhost -U sa -P ${DB_PASSWORD} -i scripts/create_database.sql",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": ["express", "sql-server", "booking"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mssql": "^10.0.1",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "express-validator": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
